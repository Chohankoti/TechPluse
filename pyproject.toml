[project]
name = "techpulse"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "chohan", email = "chohankoti333@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "jsonc-parser>=1.1.5",
    "python-dotenv>=1.2.3",
    "requests>=2.34.2",
    "tinyfish>=0.8.0",
]

[project.scripts]
techpulse = "techpulse:main"

[build-system]
requires = ["uv_build>=0.12.0,<0.13.0"]
build-backend = "uv_build"
